version: '3.8'
services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: vetsys
      POSTGRES_PASSWORD: vetsys_pass
      POSTGRES_DB: vetsysdb
    ports:
      - "15432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
